# Dependency management (since this is a purely simple c++ project without external deps, this is minimal, mostly for tests or analytics)

# Catch2 for unit testing
add_subdirectory(catch2)

# ADD BELOW HERE OTHER DEPENDENCIES IF NEEDED: